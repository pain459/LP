version: '3.8'

services:
  component2:
    build: ./C2
    ports:
      - "5001:5001"
    volumes:
      - ./C2/logs:/app/logs  # Maps logs to the host
    networks:
      - project_network

networks:
  project_network:
    driver: bridge
